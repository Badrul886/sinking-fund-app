# End Session

Before ending:
1. run relevant tests;
2. inspect Git diff;
3. update `docs/CURRENT_STATE.md`;
4. update `docs/CHANGELOG.md`;
5. update `docs/DECISIONS.md` if needed;
6. update `docs/KNOWN_ISSUES.md` if needed;
7. record unfinished work;
8. record the exact next recommended action.

Do not claim a task is complete unless verification supports it.
