# Implement Feature

Implement only the approved scope.

Rules:
- inspect existing code first;
- reuse abstractions;
- keep domain logic separate;
- avoid unrelated refactors;
- implement tests alongside critical logic;
- preserve design-system consistency.

After implementation:
- run relevant tests;
- verify UI states;
- verify platform behavior when applicable;
- update project documentation.
