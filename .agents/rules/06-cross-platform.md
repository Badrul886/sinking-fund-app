# Cross-Platform Governance

The product must feel coherent across iOS and Android without forcing identical implementation.

Share:
- domain logic;
- calculation engine;
- data models;
- business rules;
- design tokens;
- core UI patterns;
- validation.

Allow platform-specific implementations where platform conventions or APIs require them.

Verify both platforms for meaningful user-facing changes.
